{
  "mcpServers": {
    "triggo-runtime": {
      "transport": "http",
      "url": "http://localhost:8090/mcp",
      "headers": {
        "Authorization": "Bearer ${TRIGGO_RUNTIME_API_KEY}"
      }
    }
  }
}
